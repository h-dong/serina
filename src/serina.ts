const Serina = (text) => {
  const parsedData = {
    original: text,
    text: '',
    isValid: false,
    date: '',
    time: '',
    dateObject: null
  };

  return parsedData;
};

export default Serina;
